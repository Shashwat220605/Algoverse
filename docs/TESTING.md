# Testing Notes

For UI or algorithm changes, verify the affected flow manually and run the production build with:

```bash
npm run build
```

Check that navigation, execution controls, visualization state, and displayed complexity remain consistent.
