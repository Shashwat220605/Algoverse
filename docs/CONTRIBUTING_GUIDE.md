# Contributing to AlgoVerse

Keep pull requests focused on meaningful improvements to algorithms, visualizations, navigation, accessibility, documentation, or learning features.

Before opening a pull request, check the affected feature and run `npm run build` when applicable. Never commit API keys or other secrets.
